# Project Rules

## Git / GitHub

- **Do NOT push any code to GitHub without explicit permission from the user.**
  - This applies to `git push`, `git push --force`, pushing to any remote branch, or any other action that publishes local commits.
  - Committing locally is fine, but never push to a remote unless the user has explicitly asked for it.
  - When in doubt, ask first.
